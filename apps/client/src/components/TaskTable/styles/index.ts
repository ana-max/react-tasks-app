import {
  makeStyles,
} from "@material-ui/core";

const useStyles = makeStyles({
  container: {
    marginTop: "20px"
  },
  statusContainer: {
    display: "flex",
    alignItems: "center",
  },
  statusIcon: {
    marginRight: '8px',
  },
  link: {
    color: '#156FF4',
  },
  headerTitle: {
    fontSize: '16px',
    fontWeight: 600,
  },
});

export default useStyles;
